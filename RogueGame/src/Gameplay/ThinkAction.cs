
namespace RogueGame.Gameplay {
    /// <summary>
    /// Represents an set of turn actions that a thinker should perform, based on its state
    /// </summary>
    public class ThinkAction {
        public Vector2 moveStep = new Vector2(0, 0);
    }
}
